# Core (training + inference)
torch>=2.1
transformers>=4.44
accelerate>=0.30
numpy>=1.24
tqdm>=4.65

# 4-bit quantization (only needed for --load_in_4bit on 24GB cards)
bitsandbytes>=0.43

# Analysis / rebuttal scripts (newrun/)
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
openTSNE>=1.0
